@model VetApp.Models.PersonaCliente

@{
    ViewData["Title"] = "Detalles";
}

<h1>Detalles</h1>

<div>
    <h4>PersonaCliente</h4>
    <hr />
    <table class="table table-bordered">
        <tr>
            <th>@Html.DisplayNameFor(model => model.CodCliente)</th>
            <td>@Html.DisplayFor(model => model.CodCliente)</td>
        </tr>
        <tr>
            <th>@Html.DisplayNameFor(model => model.Ci)</th>
            <td>@Html.DisplayFor(model => model.Ci)</td>
        </tr>
        <tr>
            <th>@Html.DisplayNameFor(model => model.FechaAsociacion)</th>
            <td>@Html.DisplayFor(model => model.FechaAsociacion)</td>
        </tr>
    </table>
</div>
<div>
    <a asp-action="Edit" asp-route-CodCliente="@Model.CodCliente" asp-route-Ci="@Model.Ci" asp-route-FechaAsociacion="@Model.FechaAsociacion" class="btn btn-primary custom-edit-btn">Editar</a>
    <a asp-action="Index" class="btn btn-secondary custom-back-btn">Regresar a la Lista</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}

<style>
    .custom-edit-btn {
        background-color: #007bff; /* Azul */
        border-color: #007bff;
        color: white;
        margin-right: 5px;
    }

    .custom-back-btn {
        background-color: #6c757d; /* Gris */
        border-color: #6c757d;
        color: white;
        margin-top: 10px;
    }

    .btn {
        border-radius: 5px;
        padding: 10px 20px;
    }

    .form-group {
        margin-bottom: 15px;
    }

    .control-label {
        font-weight: bold;
    }

    .text-danger {
        font-size: 0.875em;
    }

    .table {
        margin-top: 20px;
    }
</style>
