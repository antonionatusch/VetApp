@using VetApp.Models
@model IEnumerable<PersonaCliente>

@{
    ViewData["Title"] = "Índice";
}

<h1>Índice</h1>

<p>
    <a asp-action="Create" class="btn btn-primary custom-create-btn">Crear Nuevo</a>
</p>
<table class="table table-striped table-bordered">
    <thead>
        <tr>
            <th>@Html.DisplayNameFor(model => model.CodCliente)</th>
            <th>@Html.DisplayNameFor(model => model.Ci)</th>
            <th>@Html.DisplayNameFor(model => model.FechaAsociacion)</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.CodCliente)</td>
                <td>@Html.DisplayFor(modelItem => item.Ci)</td>
                <td>@Html.DisplayFor(modelItem => item.FechaAsociacion)</td>
                <td>
                    <a asp-action="Edit" asp-route-CodCliente="@item.CodCliente" asp-route-Ci="@item.Ci" asp-route-FechaAsociacion="@item.FechaAsociacion" class="btn btn-primary custom-edit-btn">Editar</a>
                    <a asp-action="Details" asp-route-CodCliente="@item.CodCliente" asp-route-Ci="@item.Ci" asp-route-FechaAsociacion="@item.FechaAsociacion" class="btn btn-info custom-details-btn">Detalles</a>
                    <a asp-action="Delete" asp-route-CodCliente="@item.CodCliente" asp-route-Ci="@item.Ci" asp-route-FechaAsociacion="@item.FechaAsociacion" class="btn btn-danger custom-delete-btn">Borrar</a>
                </td>
            </tr>
        }
    </tbody>
</table>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}

<style>
    .custom-create-btn {
        background-color: #28a745; /* Verde */
        border-color: #28a745;
        color: white;
        margin-bottom: 20px;
    }

    .custom-edit-btn {
        background-color: #007bff; /* Azul */
        border-color: #007bff;
        color: white;
        margin-right: 5px;
    }

    .custom-details-btn {
        background-color: #17a2b8; /* Celeste */
        border-color: #17a2b8;
        color: white;
        margin-right: 5px;
    }

    .custom-delete-btn {
        background-color: #dc3545; /* Rojo */
        border-color: #dc3545;
        color: white;
    }

    .btn {
        border-radius: 5px;
        padding: 10px 20px;
    }

    .form-group {
        margin-bottom: 15px;
    }

    .control-label {
        font-weight: bold;
    }

    .text-danger {
        font-size: 0.875em;
    }

    .table {
        margin-top: 20px;
    }
</style>
