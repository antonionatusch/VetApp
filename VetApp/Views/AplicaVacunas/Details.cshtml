@using VetApp.Models
@model AplicaVacuna

@{
    ViewData["Title"] = "Detalles";
}

<h1>Detalles</h1>

<div>
    <h4>AplicaVacuna</h4>
    <hr />
    <table class="table table-bordered">
        <tr>
            <th>@Html.DisplayNameFor(model => model.CodMascota)</th>
            <td>@Html.DisplayFor(model => model.CodMascota)</td>
        </tr>
        <tr>
            <th>@Html.DisplayNameFor(model => model.CodVacuna)</th>
            <td>@Html.DisplayFor(model => model.CodVacuna)</td>
        </tr>
        <tr>
            <th>@Html.DisplayNameFor(model => model.FechaPrevista)</th>
            <td>@Html.DisplayFor(model => model.FechaPrevista)</td>
        </tr>
        <tr>
            <th>@Html.DisplayNameFor(model => model.FechaAplicacion)</th>
            <td>@Html.DisplayFor(model => model.FechaAplicacion)</td>
        </tr>
        <tr>
            <th>@Html.DisplayNameFor(model => model.DosisAplicada)</th>
            <td>@Html.DisplayFor(model => model.DosisAplicada)</td>
        </tr>
    </table>
</div>
<div>
    <a asp-action="Edit" asp-route-CodMascota="@Model.CodMascota" asp-route-CodVacuna="@Model.CodVacuna" asp-route-FechaPrevista="@Model.FechaPrevista" class="btn btn-primary custom-edit-btn">Editar</a>
    <a asp-action="Index" class="btn btn-secondary custom-back-btn">Regresar a la Lista</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}

<style>
    .custom-edit-btn {
        background-color: #007bff; /* Azul */
        border-color: #007bff;
        color: white;
        margin-right: 5px;
    }

    .custom-back-btn {
        background-color: #6c757d; /* Gris */
        border-color: #6c757d;
        color: white;
        margin-top: 10px;
    }

    .btn {
        border-radius: 5px;
        padding: 10px 20px;
    }

    .form-group {
        margin-bottom: 15px;
    }

    .control-label {
        font-weight: bold;
    }

    .text-danger {
        font-size: 0.875em;
    }

    .table {
        margin-top: 20px;
    }
</style>
